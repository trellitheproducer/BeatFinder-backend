fastapi
uvicorn[standard]
motor
pymongo
python-jose[cryptography]
passlib==1.7.4
bcrypt==4.0.1
httpx
python-dotenv
pydantic[email]
email-validator
python-multipart
reportlab>=4.0.0
# ── BPM detection ─────────────────────────────────────────────────────────
# All versions below MUST have pre-built Linux x86_64 wheels for the Python
# version pinned in runtime.txt (3.11.9). Source-compiling any of these on
# Render's free-tier 512MB build container will OOM and fail with
# "metadata-generation-failed".
#
# If scipy still fails to install, the Python version is wrong — check
# runtime.txt is at the repo root next to requirements.txt, AND go to
# Render → Settings → Environment → confirm PYTHON_VERSION is NOT set
# (the env var, if present, overrides runtime.txt).
numpy==1.26.4
scipy==1.11.4
llvmlite==0.41.1
numba==0.58.1
soundfile==0.12.1
audioread==3.0.1
librosa==0.10.1
